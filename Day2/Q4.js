// const arr = [1, 2, 3, 4, 5, 1];
// const threshold = Math.floor(arr.length / 3);
// const counter = {};

// for (let i = 0; i < arr.length; i++) {
//   const num = arr[i];
//   counter[num] = (counter[num] || 0) + 1;
  
//   // Check if the number repeats more than the threshold
//   if (counter[num] > threshold) {
//     console.log(num);
//   }
// }
